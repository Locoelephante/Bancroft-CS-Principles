int x = 0;
int speed = 5;
void setup()
{
size(500,500);
background(255);
}

void draw()
{
  background(255);
  fill(0);
  ellipse(250,x,100,100);
  x = x + speed;
  if(x>600)
  {
   x = -50; 
  }
  else
  {
    x = x;
  }

}
